{
  "//": [
    "Only local builds and release website staging use this file.",
    "`package-lock.json` is not committed, so npm resolves the dependency tree on first use.",
    "Live website deployments replace both files with the ones on the `gha/v0` branch of `logging-parent`."
  ],
  "dependencies": {
    "@org.apache.logging/logging-parent": "github:apache/logging-parent#gha/v0"
  }
}
