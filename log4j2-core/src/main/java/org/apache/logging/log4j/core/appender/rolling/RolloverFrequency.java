package org.apache.logging.log4j.core.appender.rolling;

/**
 *
 */
public enum RolloverFrequency {
    ANNUALLY,
    MONTHLY,
    WEEKLY,
    DAILY,
    HOURLY,
    EVERY_MINUTE,
    EVERY_SECOND,
    EVERY_MILLISECOND
}
