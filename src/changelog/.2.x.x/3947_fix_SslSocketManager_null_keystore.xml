<?xml version="1.0" encoding="UTF-8"?>
<entry xmlns="https://logging.apache.org/xml/ns"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="
           https://logging.apache.org/xml/ns
           https://logging.apache.org/xml/ns/log4j-changelog-0.xsd"
       type="fixed">
    <issue id="3947" link="https://github.com/apache/logging-log4j2/issues/3947"/>
    <description format="asciidoc">
        Corrected nullability annotations in `SslConfiguration` which lead to nullability problems in consuming code.
        In particular, when configuring a syslog appender, if only a truststore is defined, an exception is thrown during configuration time.
        This fixes that error along with various other potential null errors in related code.
    </description>
</entry>
