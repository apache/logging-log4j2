<?xml version="1.0" encoding="UTF-8"?>
<entry xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns="https://logging.apache.org/xml/ns"
       xsi:schemaLocation="https://logging.apache.org/xml/ns https://logging.apache.org/xml/ns/log4j-changelog-0.xsd"
       type="added">
  <issue id="GH-4011" link="https://github.com/apache/logging-log4j2/pull/4011"/>
  <description format="asciidoc">Add delayed compression support to `RollingFileAppender` via `minDelaySeconds` and `maxDelaySeconds` attributes on `DefaultRolloverStrategy`.
When configured, the compression action is scheduled asynchronously after a random delay in the `[minDelaySeconds, maxDelaySeconds]` range, spreading I/O load across rolling events.</description>
</entry>
